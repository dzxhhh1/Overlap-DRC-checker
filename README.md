# Overlap-DRC-checker
This program can check all overlap situations between pads and segments with their corresponding information and total numbers of overlaps in kicad version 7.0.0.

change the filename(.kicad_pcb) in main.cpp and compile using command:

g++ checkDRC.cpp main.cpp Calculator.cpp First_Part12.cpp -o main

then run:

./main
